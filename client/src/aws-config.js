const awsconfig = {
  API: {
    endpoints: [
      {
        name: "greetingsEndpoint",
        endpoint: "https://ixpd3hlwyk.execute-api.us-east-1.amazonaws.com/dev/",
      },
    ],
  },
};

export default awsconfig;
